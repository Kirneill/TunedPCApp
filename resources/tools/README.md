# GPU Tools

The app will auto-download `nvidiaProfileInspector.exe` on first GPU profile apply if it is missing.
You can still pre-place it here to avoid first-run download.

Optional hardening recommendation:
- Verify SHA-256 of the executable before release packaging.

The preset imported by the app is:
- `sq_competitive.nip`
